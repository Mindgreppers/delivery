summary: test
tasks:
    - id: kafka_test_call
      fn: com.gs.kafka
      args:
        datasource: my-kafka
        config:
          method: publish
          topic: notify-restaurant
        data:
          value: 'Hello, I am coming from Kafka.'
